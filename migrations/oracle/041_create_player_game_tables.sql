-- Oracle port of 041_create_player_game_tables.sql.
-- PostgreSQL floating-point columns are represented as Oracle NUMBER and JSON
-- payloads are represented as CLOB.
DECLARE
    v_exists NUMBER;

    PROCEDURE create_table_if_missing(p_table_name VARCHAR2, p_ddl VARCHAR2) IS
    BEGIN
        SELECT COUNT(*) INTO v_exists
          FROM user_tables
         WHERE table_name = UPPER(p_table_name);
        IF v_exists = 0 THEN
            EXECUTE IMMEDIATE p_ddl;
        END IF;
    END;
BEGIN
    create_table_if_missing('PLAYER_GAME_BATTING', q'[
        CREATE TABLE PLAYER_GAME_BATTING (
            ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
            GAME_ID VARCHAR2(20) NOT NULL,
            PLAYER_ID NUMBER(10) NOT NULL,
            PLAYER_NAME VARCHAR2(64) NOT NULL,
            TEAM_SIDE VARCHAR2(5) NOT NULL,
            TEAM_CODE VARCHAR2(10),
            BATTING_ORDER NUMBER(10),
            APPEARANCE_SEQ NUMBER(10),
            POSITION VARCHAR2(8),
            IS_STARTER NUMBER(1) DEFAULT 0 NOT NULL,
            SOURCE VARCHAR2(16),
            PLATE_APPEARANCES NUMBER(10) DEFAULT 0 NOT NULL,
            AT_BATS NUMBER(10) DEFAULT 0 NOT NULL,
            RUNS NUMBER(10) DEFAULT 0 NOT NULL,
            HITS NUMBER(10) DEFAULT 0 NOT NULL,
            DOUBLES NUMBER(10) DEFAULT 0 NOT NULL,
            TRIPLES NUMBER(10) DEFAULT 0 NOT NULL,
            HOME_RUNS NUMBER(10) DEFAULT 0 NOT NULL,
            RBI NUMBER(10) DEFAULT 0 NOT NULL,
            WALKS NUMBER(10) DEFAULT 0 NOT NULL,
            INTENTIONAL_WALKS NUMBER(10) DEFAULT 0 NOT NULL,
            HBP NUMBER(10) DEFAULT 0 NOT NULL,
            STRIKEOUTS NUMBER(10) DEFAULT 0 NOT NULL,
            STOLEN_BASES NUMBER(10) DEFAULT 0 NOT NULL,
            CAUGHT_STEALING NUMBER(10) DEFAULT 0 NOT NULL,
            SACRIFICE_HITS NUMBER(10) DEFAULT 0 NOT NULL,
            SACRIFICE_FLIES NUMBER(10) DEFAULT 0 NOT NULL,
            GDP NUMBER(10) DEFAULT 0 NOT NULL,
            AVG NUMBER,
            OBP NUMBER,
            SLG NUMBER,
            OPS NUMBER,
            ISO NUMBER,
            BABIP NUMBER,
            EXTRA_STATS CLOB,
            CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
            UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
            CONSTRAINT PK_PLAYER_GAME_BATTING PRIMARY KEY (ID),
            CONSTRAINT FK_PGB_GAME FOREIGN KEY (GAME_ID)
                REFERENCES GAME (GAME_ID) ON DELETE CASCADE,
            CONSTRAINT FK_PGB_PLAYER FOREIGN KEY (PLAYER_ID)
                REFERENCES PLAYER_BASIC (PLAYER_ID),
            CONSTRAINT UQ_PGB_GAME_PLAYER UNIQUE (GAME_ID, PLAYER_ID)
        )
    ]');

    create_table_if_missing('PLAYER_GAME_PITCHING', q'[
        CREATE TABLE PLAYER_GAME_PITCHING (
            ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
            GAME_ID VARCHAR2(20) NOT NULL,
            PLAYER_ID NUMBER(10) NOT NULL,
            PLAYER_NAME VARCHAR2(64) NOT NULL,
            TEAM_SIDE VARCHAR2(5) NOT NULL,
            TEAM_CODE VARCHAR2(10),
            IS_STARTING NUMBER(1) DEFAULT 0 NOT NULL,
            APPEARANCE_SEQ NUMBER(10),
            SOURCE VARCHAR2(16),
            INNINGS_OUTS NUMBER(10) DEFAULT 0 NOT NULL,
            HITS_ALLOWED NUMBER(10) DEFAULT 0 NOT NULL,
            RUNS_ALLOWED NUMBER(10) DEFAULT 0 NOT NULL,
            EARNED_RUNS NUMBER(10) DEFAULT 0 NOT NULL,
            HOME_RUNS_ALLOWED NUMBER(10) DEFAULT 0 NOT NULL,
            WALKS_ALLOWED NUMBER(10) DEFAULT 0 NOT NULL,
            STRIKEOUTS NUMBER(10) DEFAULT 0 NOT NULL,
            HIT_BATTERS NUMBER(10) DEFAULT 0 NOT NULL,
            WILD_PITCHES NUMBER(10) DEFAULT 0 NOT NULL,
            BALKS NUMBER(10) DEFAULT 0 NOT NULL,
            WINS NUMBER(10) DEFAULT 0 NOT NULL,
            LOSSES NUMBER(10) DEFAULT 0 NOT NULL,
            SAVES NUMBER(10) DEFAULT 0 NOT NULL,
            HOLDS NUMBER(10) DEFAULT 0 NOT NULL,
            DECISION VARCHAR2(2),
            BATTERS_FACED NUMBER(10) DEFAULT 0 NOT NULL,
            ERA NUMBER,
            WHIP NUMBER,
            FIP NUMBER,
            K_PER_NINE NUMBER,
            BB_PER_NINE NUMBER,
            KBB NUMBER,
            EXTRA_STATS CLOB,
            CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
            UPDATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
            CONSTRAINT PK_PLAYER_GAME_PITCHING PRIMARY KEY (ID),
            CONSTRAINT FK_PGP_GAME FOREIGN KEY (GAME_ID)
                REFERENCES GAME (GAME_ID) ON DELETE CASCADE,
            CONSTRAINT FK_PGP_PLAYER FOREIGN KEY (PLAYER_ID)
                REFERENCES PLAYER_BASIC (PLAYER_ID),
            CONSTRAINT UQ_PGP_GAME_PLAYER UNIQUE (GAME_ID, PLAYER_ID)
        )
    ]');
END;
/
