name: KBO Weekly Maintenance

on:
  schedule:
    - cron: '0 20 * * 0'  # 월요일 05:00 KST (일 20:00 UTC)
  workflow_dispatch:
    inputs:
      profile_limit:
        description: 'Max profiles to enrich'
        required: false
        default: '500'

concurrency:
  group: kbo-db-write-operations
  cancel-in-progress: false

env:
  PYTHONPATH: ${{ github.workspace }}

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: ./.github/actions/kbo-job-setup
        with:
          playwright: 'true'
          init-db: 'true'

      - name: Refresh Source Snapshots
        continue-on-error: true
        env:
          OCI_DB_URL: ${{ secrets.OCI_DB_URL }}
        run: |
          if [ -z "${OCI_DB_URL:-}" ]; then
            echo "OCI_DB_URL not set; skipping source snapshot refresh"
            exit 0
          fi
          DATABASE_URL="$OCI_DB_URL" python3 -m src.cli.refresh_source_snapshots \
            --all --max-hours 168

      - name: Run Weekly Maintenance & Sync
        env:
          OCI_DB_URL: ${{ secrets.OCI_DB_URL }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        run: |
          LIMIT="${{ github.event.inputs.profile_limit || '500' }}"
          python3 -m src.cli.run_weekly_maintenance \
            --profile-limit "$LIMIT" --sync

      - name: Run SLA Weekly Report
        env:
          DATABASE_URL: ${{ secrets.OCI_DB_URL }}
        run: |
          python3 -c "
          from src.monitoring.sla_tracker import SlaTracker
          from src.db.engine import SessionLocal
          with SessionLocal() as session:
              tracker = SlaTracker(session)
              tracker.print_weekly_report()
          "

      - name: Run Historical Completeness Audit
        continue-on-error: true
        env:
          DATABASE_URL: ${{ secrets.OCI_DB_URL }}
        run: |
          python3 -m scripts.maintenance.audit_completeness_2009_2025 \
            --start-year 2009 --end-year 2025 \
            --output-dir data/audit

      - name: Upload Historical Completeness Audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: historical-completeness-audit
          path: data/audit/
          if-no-files-found: warn

      - uses: ./.github/actions/notify
        if: always()
        with:
          status: ${{ job.status }}
          channels: telegram
